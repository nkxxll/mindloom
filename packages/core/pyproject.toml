[project]
name = "mindloom-core"
version = "0.1.0"
description = "Mindloom shared core library"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.0",
    "httpx>=0.28",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
